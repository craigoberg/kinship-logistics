/** Minimum chars for timeline / manage-shell notes. */
export const MIN_TIMELINE_NOTE = 10;

/** Minimum chars for domain evidence references on resolve. */
export const MIN_EVIDENCE = 6;

/** @deprecated Domain justification comes from the shell note (MIN_TIMELINE_NOTE). */
export const MIN_DOMAIN_JUSTIFICATION = 10;
